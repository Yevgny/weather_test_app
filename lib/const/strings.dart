import 'dart:ui';

abstract class AppStrings {
  static String appVersion = '1.0.0+1';

  static const locale = Locale('en', 'US');
  static const appName = 'Weather Test';
  static const unknownError = 'Some unknown error happens';
}
